CREATE TABLE AnswerType(
   id INTEGER PRIMARY KEY,
   name TEXT NOT NULL,
   weight INTEGER DEFAULT 1

);

CREATE TABLE Template(
   id INTEGER PRIMARY KEY,
   type_id INTEGER NOT NULL,
   content TEXT NOT NULL,
   FOREIGN KEY(type_id)
       REFERENCES AnswerType(id)

);

CREATE TABLE WordCategory(
   id INTEGER PRIMARY KEY,
   name TEXT NOT NULL UNIQUE

);

CREATE TABLE Word(
   id INTEGER PRIMARY KEY AUTOINCREMENT,
   category_id INTEGER NOT NULL,
   content TEXT NOT NULL,
   FOREIGN KEY(category_id)
       REFERENCES WordCategory(id)

);

CREATE TABLE TemplateSlot(
   id INTEGER PRIMARY KEY AUTOINCREMENT,
   template_id INTEGER NOT NULL,
   slot_name TEXT NOT NULL,
   category_id INTEGER NOT NULL,


   UNIQUE(template_id, slot_name),


   FOREIGN KEY(template_id)
       REFERENCES Template(id),


   FOREIGN KEY(category_id)
       REFERENCES WordCategory(id)

);

INSERT INTO AnswerType (id, name, weight) VALUES
   (1, '肯定',        20),
   (2, '否定',        20),
   (3, '等待',        30),
   (4, '警告',        15),
   (5, '反思',        15);

INSERT INTO Template (id, type_id, content) VALUES
   (1,1, '{thing}會帶來好結果。'),
   (2,2, '現在不要相信{thing}。'),
   (3,3, '關於{thing}還需要等待。'),
   (4,4, '小心{thing}背後的代價。'),
   (5,5, '或許你該重新思考{thing}。'),
   (6,4,'{person}將改變你的{thing}。'),
  (7,4,'在{place}等待。'),
  (8,5,'或許該先{action}。');

INSERT INTO WordCategory (id, name) VALUES
   (1, 'thing'),
   (2, 'person'),
   (3, 'place'),
   (4, 'action');

INSERT INTO Word (id, category_id, content) VALUES
   (1,1, '機會'),
   (2,1, '決定'),
   (3,1, '約定'),
   (4,1, '旅程'),
   (5,2, '某個人'),
   (6,2, '舊識'),
   (7,3, '家'),
   (8,4, '睡一覺');

INSERT INTO TemplateSlot (template_id, slot_name, category_id) VALUES
   (1, 'thing',1),
   (2, 'thing',1),
   (3, 'thing',1),
   (4, 'thing',1),
   (5, 'thing',1),
   (6, 'person',2),
   (6, 'thing',1),
   (7,'place',3),
   (8,'action',4);



CREATE INDEX idx_template_type

ON Template(type_id);



CREATE INDEX idx_word_category

ON Word(category_id);





| id | type_id | content          |

| -- | ------- | ---------------- |

| 1  | 1       | {thing}會帶來好結果。   |

| 2  | 2       | 現在不要相信{thing}。   |

| 3  | 3       | 關於{thing}還需要等待。  |

| 4  | 4       | 小心{thing}背後的代價。  |

| 5  | 5       | 或許你該重新思考{thing}。 |

